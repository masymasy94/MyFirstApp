<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myfirstapp">

    <application
        android:allowBackup="true"
        android:name=".MyApp"
        android:icon="@mipmap/ic_launcher_ddapp_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_ddapp_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AddEnchantmentActivity"></activity>
        <activity android:name=".AddMonsterActivity" />
        <activity android:name=".AddItemActivity" />
        <activity android:name=".GeneraNegoziActivity" />
        <activity android:name=".EnchantmentsActivity" />
        <activity android:name=".ItemsActivity">
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>
        <activity android:name=".MonstersActivity" />
        <activity android:name=".ActivityDM" />
        <activity android:name=".ActivityHomePage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>